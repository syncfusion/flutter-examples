enum UserInteractions { add, edit, delete }

enum FinancialPages { goals, budgets }

enum TransactionType { all, income, expense, savings }

enum CardType { gridCard, insightCard }

enum WelcomeScreens { setupPage, importPage }
